
同一个dom 多个 css 类 有效性是按照css代码中的顺序生效的，即越靠后的css类，执行力越强，权重越高， 而不是dom上设置的css类顺序决定

css类在应用在 dom之前， 即使类存在于css脚本中，也不会执行（如请求图片）

css类在应用在 dom之前， 即使类存在于css脚本中，首次也不会执行（如请求图片）

css类在已经即使应用在 dom， 如果dom存在其他同效果类， 权重高的，或同权重脚本中靠后的优先生效， 而权重低的，首次不会执行（即类内部被同类覆盖的样式不会执行，即css树节点也没生效）（如图片请求）

首次： 页面初始渲染，类第一次应用在dom， 后续即使移除该类，也会缓存在css树节点

可能导致的问题：

动态添加类， 大型背景变换首次会出现闪屏

解决办法，

1.dom 上应用将用到的类，渲染完成，移除该类，预执行css类
2.影藏dom css类预缓存


js 预缓存资源； css如果已存在css树节点， 引用资源，css不会发起请求 devtools不可观察到请求


js 预缓存资源； css如果还未存在css树节点， 引用资源，css会向缓存发起请求，devtools可观察到请求

预缓存方案：

通过 js 预缓存： 可缓存任何资源， 也可对css内部资源预缓存， 避免css再次从缓存中请求，一般为图片

通过css 预缓存，对于即现内容，需要对css预执行，一般为大型图片，防闪屏；对于非即现内容，可不予执行css, 一般为小模块图片

